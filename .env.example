DATABASE_URL="file:./dev.db"
SESSION_SECRET="change-me"

# Geocoding (OpenStreetMap Nominatim). No API key needed, but the usage policy
# requires an identifying User-Agent. Swap these to point at another provider.
GEOCODING_BASE_URL="https://nominatim.openstreetmap.org"
GEOCODING_USER_AGENT="NagarSetu/1.0 (contact: you@example.com)"
GEOCODING_COUNTRY_CODES="in"
